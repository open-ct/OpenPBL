version: '3.1'
services:
  openpbl:
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    depends_on:
      - db
  db:
    restart: always
    image: mysql:8.0.25
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - /usr/local/docker/mysql:/var/lib/mysql